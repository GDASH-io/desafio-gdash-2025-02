# ============================================
# GDASH Weather + IA - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha com seus valores
# Para rodar fora do Docker, configure cada serviço com suas respectivas variáveis

# ============================================
# BACKEND - NestJS API
# ============================================

# MongoDB
# Formato: mongodb://[usuário]:[senha]@[host]:[porta]/[database]?authSource=admin
# Exemplo local: mongodb://localhost:27017/weather_db
# Exemplo com autenticação: mongodb://root:rootpassword@localhost:27017/weather_db?authSource=admin
MONGODB_URI=mongodb://localhost:27017/weather_db

# JWT Secret (use uma string longa e aleatória em produção)
JWT_SECRET=supersecretjwtkey-change-this-in-production

# Usuário administrador padrão (criado automaticamente na inicialização)
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=123456

# TMDB API Key
# Obtenha em: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-api-key-here

# Groq API Key
# Obtenha em: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here

# ============================================
# RABBITMQ (usado por Python Collector e Go Worker)
# ============================================

# Host do RabbitMQ (localhost se rodar localmente)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# ============================================
# PYTHON COLLECTOR
# ============================================

# URL da API Open-Meteo (geralmente não precisa mudar)
OPEN_METEO_API_URL=https://api.open-meteo.com/v1/forecast

# Coordenadas geográficas para coleta (latitude, longitude)
# Exemplo: Salvador, BA
LATITUDE=-12.9714
LONGITUDE=-38.5014

# Intervalo de coleta em segundos (3600 = 1 hora)
COLLECTION_INTERVAL=3600

# ============================================
# GO WORKER
# ============================================

# URL da API NestJS (localhost se rodar localmente)
NESTJS_API_URL=http://localhost:3000

# ============================================
# FRONTEND - React
# ============================================

# URL base da API NestJS
# Para desenvolvimento local: http://localhost:3000
# Para produção: ajuste conforme necessário
VITE_API_BASE_URL=http://localhost:3000

# ============================================
# INSTRUÇÕES DE USO
# ============================================
#
# 1. BACKEND (NestJS):
#    - Crie um arquivo .env na pasta nestjs-api/
#    - Copie as variáveis: MONGODB_URI, JWT_SECRET, DEFAULT_ADMIN_EMAIL, 
#      DEFAULT_ADMIN_PASSWORD, TMDB_API_KEY, GROQ_API_KEY
#
# 2. PYTHON COLLECTOR:
#    - Configure as variáveis de ambiente no seu sistema ou crie um .env
#    - Variáveis: RABBITMQ_HOST, RABBITMQ_PORT, RABBITMQ_USER, RABBITMQ_PASS,
#      OPEN_METEO_API_URL, LATITUDE, LONGITUDE, COLLECTION_INTERVAL
#
# 3. GO WORKER:
#    - Configure as variáveis de ambiente no seu sistema ou crie um .env
#    - Variáveis: RABBITMQ_HOST, RABBITMQ_PORT, RABBITMQ_USER, RABBITMQ_PASS,
#      NESTJS_API_URL
#
# 4. FRONTEND (React):
#    - Crie um arquivo .env na pasta react-frontend/
#    - Variável: VITE_API_BASE_URL
#
# NOTA: Se usar Docker Compose, essas variáveis são configuradas no
# docker-compose.yml (siga o docker-compose.example.yml ). Este arquivo é para desenvolvimento local sem Docker. 

