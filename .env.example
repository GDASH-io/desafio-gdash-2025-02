# Variáveis de ambiente exemplo para a stack GDASH Challenge
# Renomeie para .env em cada serviço conforme necessário.

# URL de conexão do MongoDB usado pela API NestJS
MONGO_URI=mongodb://mongo:27017/gdash

# URL de conexão para o RabbitMQ (worker Go e coletor Python)
# Use credenciais padrão do container rabbitmq (guest/guest)
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/

# Segredo usado para assinar tokens JWT (auth backend)
JWT_SECRET=changeme

# Chave para ingestão do worker Go (usada no header X-API-KEY ou Bearer)
INGEST_API_KEY=dev_ingest_key

# Credenciais do usuário administrador inicial (seed no backend)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=123456

# Coordenadas (latitude e longitude) para coleta de clima (ex.: Campina Grande - PB)
WEATHER_API_LAT=-7.2306
WEATHER_API_LON=-35.8811

# Fila usada pelos serviços de clima
WEATHER_QUEUE=weather.raw

# URL da API NestJS (acessível via rede do docker-compose)
API_URL=http://api:3000
# Caminho do endpoint de ingest de clima
API_PATH=/weather/logs
# Token opcional para autenticar ingest (se o backend exigir)
# API_TOKEN=dev_ingest_key

# (Opcional) Trocar por outra cidade ajustando LAT/LON.
# Em produção altere JWT_SECRET e ADMIN_PASSWORD para valores seguros.
